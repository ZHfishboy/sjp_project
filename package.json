{
  "name": "calcmaster",
  "version": "1.0.0",
  "private": true,
  "description": "CalcMaster - Multi-function Calculator Platform",
  "scripts": {
    "dev": "concurrently -n BE,FE -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
